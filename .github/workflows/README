Automated workflow to generate addon release.

Steps to release:

- commit and push to origin (distant)
- create a new tag version: git tag vXXX.XXX
- push the tag to origin: git push origin vXXX.XXX (<= release will be generated here)
